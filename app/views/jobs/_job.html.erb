<strong><%= job.name %> </strong>
<%= "Department: #{job.department.name}" unless job.department.nil? %>
<%= link_to "edit", edit_job_path(job) %>
<%= link_to "delete", job, :method => :delete,
            :confirm => "You sure?",
            :title => job.name %>

<%= link_to "add pay sheet", new_pay_sheet_path(:job_id => job) if current_user.admin || current_user.supervisor? %>